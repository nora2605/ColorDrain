# Dialog File Format

Line by Line text/instruction

- `>Speaker` (plain `>` disables the box)
- `*sprite` (changes current speaking sprite)
- `§variable§` (inside text, inserts variables like player name)
- `#*` is a blocking sprite change, it does not automatically continue.